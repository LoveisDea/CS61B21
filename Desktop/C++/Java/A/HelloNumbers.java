public class HelloNumbers {
    public static void main(String[] args){
        int x;
        x = 0;
        while(x < 10){
            System.out.println(x);
            x = x+1;
        }
    }
}

/**
1. The variables must be declared before being used.
2. The variables must have a specific type.
3. The type of a variable can never be changed.
4. Types are verified before the code runs.=> If there are type issues, the code will not compile.
 */
